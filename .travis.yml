language: rust
rust:
  - nightly-2019-07-19
  - nightly

os:
  - linux

matrix:
  allow_failures:
    - rust: nightly
  fast_finish: true

cache: cargo

script:
  - cargo build --verbose --all
  - cargo test --verbose --all
  - cargo doc --verbose --all --no-deps
